# Ubuntu 20.04 / Python 3.8 — torch 는 Dockerfile 에서 cu118 wheel 로 설치한다.
numpy==1.24.4
scipy==1.10.1
matplotlib==3.7.5
opencv-python==4.9.0.80
pillow==10.4.0
PyYAML==6.0.1
tqdm
tensorboard==2.14.0   # 실험 추적. wandb(외부 계정 의존)는 쓰지 않는다.
