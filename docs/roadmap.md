# 역할 분담과 작업 순서

> **문서 역할:** 제안 — 팀 논의용 초안. 합의되면 [architecture.md](architecture.md) §5 에 반영한다
> **담당:** 안승현
> **최종 수정:** 2026-09-23

## 0. 전제

- 개발 가능 주는 **3·4·5 / 9 / 11·12·13 — 흩어진 7주** ([course.md](course.md) §3)
- **10/05~11/01 은 시험 기간으로 끊긴다.** 1블록은 실질 2주 남았다
- 기반 논문·인지 모델 **미정**
- ASMC 이식은 끝났다 ([porting-from-asmc.md](porting-from-asmc.md)). 뼈대 위에서 시작한다

## 1. 현재 분담 검토

적어 주신 분담은 대체로 타당하다. **MORAI 경험이 있는 2명이 뼈대를 세우고 나머지 3명이
그 위에서 각자 축을 맡는 구도**는 이 팀 구성에서 맞다. 다만 네 가지를 조정하는 편이 낫다.

### ① 하수영 ↔ 김동현 의존이 서로를 기다린다

논문이 정해져야 라벨 정의가 나오고, 라벨 정의가 나와야 수집 코드가 완성된다. 그런데
1블록이 2주다.

**해법: 라벨과 무관한 부분을 먼저 고정한다.** 김동현의 1블록 목표를
"4카메라 + `/Ego_topic` + `/gps` 를 시각 동기화해서 프레임 단위로 떨어뜨리는 것"까지로
자른다. 어떤 논문을 고르든 필요한 부분이고, 라벨 스키마는 비워 둔다.

이식된 `data_collection` 이 이미 이 형태다 — 김동현은 처음부터 설계하는 게 아니라
**동결된 manifest 스키마를 만족시키는 일**을 하면 된다. 범위가 명확해지고 안승현과
인터페이스 협상이 필요 없다.

### ② 강도균의 과제가 "몸풀기"가 아니라 **크리티컬 패스**다

KATRI 맵에 주차면 기하가 **없다** ([katri-map.md](katri-map.md)). 원본 export 에는 있었지만
우리가 못 받았다. 받기 전까지는 **강도균이 만들 GT 가 팀의 유일한 주차 GT** 다.

**해법 두 가지.**

- 1블록 첫 과제를 좁힌다: **"주차장에서 Semantic 캡처 한 장을 떠서 주차칸 선이 어떤
  클래스로 칠해지는지 확인한다."** yes 면 GT 파이프라인 전체가 열리고, no 면 팀 전체
  접근이 바뀐다. 가장 싸고 가장 결정적이다.
- 산출 포맷을 **지금** 고정한다: `src/vip3_hd_map/config/vip3_parking_spaces.json`
  (MORAI `ParkingSpace` 스키마). 그래야 김동현·하수영이 그걸 전제로 설계한다.
  스키마와 좌표 찍는 방법은 [SCHEMA.md](../src/vip3_hd_map/config/vip3_parking_spaces.SCHEMA.md) 에 적어 뒀다.

그리고 **강도균과 하수영의 역할 경계를 명시**한다 — 둘 다 "차선을 뽑는" 일이라 겹칠 위험이
있다. **강도균 = semantic GT(정답) 생산 / 하수영 = RGB 추론(예측) 소비.** 이렇게 나누면
강도균의 GT 가 곧 김동현 파이프라인의 라벨 입력이 되어 3명이 직렬로 연결된다.

### ③ 하수영의 논문 선정이 강도균의 센서셋을 블로킹한다

주차 슬롯 검출 계열(DMPR-PS, PSDet, AVM 기반)은 **대부분 fisheye 어라운드뷰 4대 합성
영상을 입력으로 가정**한다. 논문을 고르는 순간 센서셋이 결정된다.

**해법: 하수영에게 먼저 데드라인을 준다.** "2블록(10/26~11/01) 시작 전까지 후보 3개로
압축" 정도. 그리고 후보를 고를 때 **입력 형태(핀홀 4대 / fisheye AVM / BEV 변환)** 를
반드시 같이 적게 한다.

> 참고: 현재 BEV 호모그래피는 `0 < FOV < 180` 을 요구한다. **진짜 fisheye(≥180°)는 핀홀
> 모델로 못 다룬다.** 어안 모델을 따로 넣어야 하고 그건 별도 공수다.

### ④ 팀장 본인 작업의 난이도 순서가 뒤집혀 있다

적어 주신 목표가 "TwinLite 를 4개 카메라에서 추론"인데, **추론은 GPU 만 있으면 되는
반복 작업**이고 실제 난점은 **후방 카메라를 포함한 BEV 합성**이다.

이식하면서 이미 대부분 처리했다 (`drivable_bev` 67 tests OK, 후방 yaw 180 커버리지
27.4 % 확인). 남은 건 **실기에서 지면 격자가 아스팔트에 얹히는지 보는 것** 하나다.
그게 틀리면 그 뒤가 전부 무의미하므로 **여기를 먼저 뚫는다.**

## 2. 아무도 안 맡고 있는 것 — 주차 성공 판정

Pass/Fail 과목에 12/08 시연이 있는데, **"주차가 성공했다"를 무엇으로 판정할지** 정해져
있지 않다. 목표 주차면 대비 차체 4코너 오차 · 최종 heading 오차 · 소요 시간 · 충돌 여부.

이걸 계산하는 노드가 없으면 3블록(11/09~11/29)에 **개선 방향을 정량으로 못 정한다.**
11/17·11/24 점검에서 보여줄 숫자도 없다.

**제안: `vip3_eval` — 김동현.** 데이터 파이프라인 담당이 실험 분석까지 맡기로 했으니
자연스럽다. `/Ego_topic` pose + 목표 ParkingSpace → 코너 오차·heading 오차·소요시간·충돌을
CSV + RViz 마커로 낸다.

## 3. 조정된 분담

| 담당 | 영역 | 경로 |
|---|---|---|
| **안승현** | 이식 총괄 · BEV · 시각화 · 4카메라 추론 | `src/perception/drivable_bev/`, `src/vip3_hd_map/`, `src/perception/camera_semantic_perception/` |
| **장원태** | 공용 인프라 · 시뮬 연동 · 제어 캘리브레이션 | `docker/`, `scripts/`, `config/`, `src/vip3_bringup/`, `src/vip3_vehicle_state/` |
| **강도균** | 센서셋 · **Semantic GT 생산** · 주차면 GT | `config/VIP3_sensor_set_*.json`, `src/vip3_hd_map/config/vip3_parking_spaces.json` |
| **김동현** | 데이터 파이프라인 · **평가 하네스** | `src/data_collection/`, `src/vip3_eval/`(신규) |
| **하수영** | 인지 모델 · 기반 논문 | `src/perception/twinlite_morai/`, `scripts/*twinlite*` |

`docker/` · `scripts/` · `config/` 는 **안승현·장원태 공동 소유**로 못박는다. 전원이 매일
쓰지만 바뀌면 전원이 멈추는 경로다.

> **센서셋 ↔ 검증 스크립트 결합을 끊었다.** 강도균이 센서셋을 바꾸면 토픽·주기 상수가 같이
> 바뀌어야 한다. `config/vip3_topics.yaml` 을 정본으로 두고 `verify_morai_topics.py` 가
> 그걸 읽게 해서 이 결합을 없앴다. 센서셋을 바꿀 때 같이 고칠 파일은 다음 넷이다:
> `config/VIP3_sensor_set_v1_ros.json` · `config/vip3_topics.yaml` ·
> `src/perception/drivable_bev/config/cameras_vip3_v1.yaml`(+`source_sha256`) ·
> `src/vip3_bringup/launch/static_tf.launch`(`tools/dump_static_tf.py` 로 재생성).

## 4. 1블록 (~10/04, 실질 2주) — 우선순위 순

**전부 "설계를 되돌릴 수 있는 정보"를 먼저 얻는 순서다.** 코드를 더 쓰는 것보다 이 답들이
먼저다.

| # | 일 | 담당 | 공수 | 왜 이 순서 |
|---:|---|---|---:|---|
| 1 | **시뮬 연결 확정** — 센서 `commType` ROS 값 확인, 카메라 4개 토픽 확인 | 안승현·장원태 | 0.5d | **다른 전부의 전제.** 카메라가 ROS 로 안 올라오면 ROS-only 전제 자체가 무너진다 |
| 2 | **rosbridge 4카메라 실측** `verify_morai_topics.py --duration 30` | 장원태 | 0.5d | 여기서 막히면 수집 설계 전체가 바뀐다 |
| 3 | **`/ctrl_cmd` 켜고 기어 확인** — R 이 들어가는가 | 안승현 | 0.5d | 주차 가능 여부 자체를 결정. 현재 네트워크 프리셋에 `/ctrl_cmd` 가 **없다** |
| 4 | **Semantic 주차칸 클래스 확인** | 강도균 | 1d | 팀 전체 접근을 가르는 실험 |
| 5 | **`parking_space_set.json` 요청** | 장원태 | 0.5h | 메일 한 통. 받으면 4번 부담이 사라진다 |
| 6 | **BEV 지면 격자 게이트** — `rgb_bev.launch` 로 눈 확인 | 안승현 | 1d | 틀리면 그 뒤가 전부 조용히 무의미해진다 |
| 7 | 4카메라 프레임 동기 수집 골격 (라벨 스키마 비움) | 김동현 | 2d | 논문과 무관하게 필요 |
| 8 | TwinLite 4카메라 추론 + 후방 단독 확인 | 안승현 | 1d | 후방에서 뭐가 나오는지 아무도 본 적 없다 |
| 9 | 기반 논문 후보 3개 압축 (입력 형태 명시) | 하수영 | 상시 | 2블록에 결론 |

### 10/04 전에 반드시 커밋할 것

**"시뮬이 연결되고 BEV 가 뜨는 상태" + 다음 세 문서의 실측값.**

- [simulator.md](simulator.md) §7 체크리스트 8항목의 **답**
- [sensors.md](sensors.md) §6 체크리스트의 **답**
- [msgs-26r1.md](msgs-26r1.md) §3 의 CTE 필드가 주차장에서 채워지는지

**3주 공백 뒤 복귀할 때 이 답들이 없으면 연결부터 다시 뚫어야 한다.** 반쯤 고친 상태로
두면 복귀에만 한 주가 든다.

## 5. 2블록 (9주차, 10/26~11/01) — 1주뿐

- 공백 복귀, 빌드·연결 재확인 (위 3문서가 있으면 반나절)
- **기반 논문 확정** — 하수영·김동현. 이게 늦으면 3블록이 무너진다
- 논문이 정한 입력 형태에 맞춰 **센서셋 v2 확정** — 강도균
- 11/10 계획서에 쓸 진행 내용 정리
- 11/03 중간고사 (이론 필기 — 개발과 별개)

## 6. 3블록 (11·12·13주차, 11/09~11/29)

- **11/10** 계획서 발표 — 지금까지 만든 것 + 남은 3주 계획
- 주차 경로 생성 + 저속 후진 제어
- `vip3_eval` 로 성공률·오차를 숫자로 — 11/17·11/24 점검 재료
- **11/29 기능 동결.** 이후는 안정화만

### 제어에 대한 솔직한 의견

ASMC 의 `mpc_controller` 는 **저속 후진 주차에 재사용할 게 없다.** 기어 개념이 아예 없고
(`grep gear` 0건), 레퍼런스 검증기가 곡률 상한을 `tan(40°)/wheelbase` 로 걸어서 **주차
경로는 상시 거절**된다. 커스프(전진↔후진 전환점)에서 곡률 연속성 검사도 실패한다.

0.5~1.5 m/s 에서는 MPC 의 예측 지평 이득이 사실상 0 이다.
**부호 있는 lookahead pure-pursuit 약 200줄이 1~2일**이면 되고 디버깅도 쉽다.
(전진 `δ = atan(2L·sin α / Ld)`, 후진은 뒷축 기준으로 부호 반전.)

ASMC 에서 가져올 것은 코드가 아니라 설계 두 가지다.

1. **fail-safe 표** — 입력이 stale/invalid 거나 solver 가 실패하면 그 사이클은 무조건 STOP
2. **클램프 후 `longlCmdType` 명시** — 26.R1 에서는 `front_steer` 에 넣고
   **`rear_steer = 0.0` 을 반드시 명시적으로 채운다**

## 7. 안 가져온 것과 그 이유

| 대상 | 왜 |
|---|---|
| `morai_udp_bridge` / `morai_sensor_bridge` | ROS 단일 전송으로 간다 |
| `frenet_planner` / `mpc_controller` / `behavior_planner` | 고속 주행용. 위 §6 참고 |
| `traffic_light` / `lidar+sensorfusion` | 주차 scope 밖 |
| `tools/perception_eval` | **스텁이다.** README 가 직접 "인터페이스·exit code 만 구현"이라고 적었다 |
| ASMC 차선 벡터화 노드 | x 단조증가 선만 추적한다. 주차칸 **진입선은 차축에 수직**이라 구조적으로 못 잡는다 |
| K-City 2025 경로 큐레이션 | 대회 전용 |

## 8. 나중에 볼 만한 것

- `tools/aim_scenario_runner` — 주차 시작 자세를 반복 재현. 수집·평가 반복성에 큰 이득이지만
  gRPC 의존이 붙는다. **ROS 대안 두 개를 먼저 시도할 것**: `/ScenarioLoad` 로 고정 시작
  상태 리로드, `/Service_MoraiEventCmd` 의 `set_pause` + 수동 배치
- `behavior_planner` 의 조향·페달 캘리브레이션 스크립트 2개 — 저속·후진 δ→κ 와 브레이크
  데드밴드 실측. 제어기 설계의 입력이 된다 (0.5d)
- LiDAR 점유로 BEV 보정 — 지면 평면 가정 때문에 주차된 옆 차가 시선 방향으로 길게 번진다.
  `/velodyne_points` 는 이미 있다
