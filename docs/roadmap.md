# 역할 분담과 작업 계획

> **문서 역할:** 제안 — 팀 논의용. 합의되면 [architecture.md](architecture.md) §5 에 반영한다
> **담당:** 안승현
> **최종 수정:** 2026-09-23

## 0. 이 문서를 읽는 법

**팀원 각자 §5 에서 자기 이름을 찾고, §7 의 "첫 과제"부터 시작하면 된다.**
§1~§4 는 왜 그렇게 나눴는지의 근거다.

읽기 전 전제 두 개:

- 개발 가능 주는 **4·5 / 9 / 11·12·13 / 14 — 흩어진 7주** ([course.md](course.md) §3)
- **발표가 세 번이다** — 10/13 중간발표-1 · 11/17 중간발표-2 · 12/15 최종발표.
  각 발표 직전에 "이번에 새로 되는 것"이 하나씩 있어야 한다
- 1블록은 3주차가 지나서 **실질 2주**(~10/04)뿐이고, 그 뒤 10/13 발표가 곧바로 온다
- ASMC 이식은 끝났다 ([porting-from-asmc.md](porting-from-asmc.md)). 빈 저장소가 아니라
  **BEV·수집·학습 뼈대가 이미 도는 상태**에서 시작한다 (테스트 193개 통과)

---

## 1. 목표를 최소 단위로 자른다

**주차칸을 고르지 않는다. 칸 하나를 미리 지정해 두고, 그 칸을 인식해서 주차한다.**

빈 칸 탐색·다중 후보 선택·주차장 순회는 **전부 범위 밖**이다. 논문들이 푸는 문제의
대부분이 "어느 칸에 댈지 고르는 것"인데, 그건 7주에 팀 전체가 매달려도 시연까지 못 간다.
게다가 **틀렸을 때 어디가 틀렸는지 알 수가 없다.**

### 마일스톤 3개

| | 이름 | 무엇이 되면 끝인가 | 인지 | 목표 시점 |
|---|---|---|---|---|
| **M1** | 인식 없이 주차 | 고정 시작자세 → 손으로 적은 경유점 → 차가 지정 칸에 들어간다 | 0 % | 10/04 (10/13 발표 재료) |
| **M2** | 보기만 한다 | 차가 주차칸 선을 **인식해서 칸 자세를 출력**한다. 정답과의 오차를 숫자로 잰다 | 100 % (오픈루프) | 11/15 (11/17 발표 재료) |
| **M3** | 본 것으로 주차 | M2 의 출력이 M1 의 목표 자세를 대체한다. 성공률을 잰다 | 100 % (폐루프) | 12/06 기능 동결 |

**M1 에 인지가 0 %인 것이 핵심이다.** 차를 먼저 움직여 놓으면, 그 뒤 인지가 틀렸을 때
"인지가 틀렸다"고 바로 말할 수 있다. 반대로 하면 끝까지 아무것도 못 움직인다.

M1 은 전체 배선(제어·평가·시작자세 재현)을 다 태우면서도 **학습이 하나도 필요 없다.**
그 사이에 나머지 셋이 학습 쪽을 병렬로 판다.

> **왜 M1 이 시시해 보여도 중요한가.** 발표가 **10/13 · 11/17 · 12/15 세 번**이다.
> 첫 발표가 10/13 인데 그때 "차가 칸에 들어간다"가 안 되면 보여줄 게 없다.
> M1 을 10/04 에 끝내 두면 **최악의 경우에도 세 번 다 시연할 것이 남는다.**
> 그 뒤는 전부 개선이다.

---

## 2. 학습 기반을 어디에 넣나

### 전부 학습으로 하면(end-to-end) 7주에 디버깅이 안 된다

`e2e-parking-carla`(IV 2024)·`ParkingE2E`(IROS 2024)가 그 방식이다. 영상을 넣으면 조향·페달이
나온다. 멋있지만 **중간 출력이 없어서 틀렸을 때 볼 게 없고**, 두 논문 다 시뮬에서 수만 개
에피소드를 굴려 학습했다.

### 이식해 온 TwinLiteNet+ 는 주차에 맞지 않는다

가져왔으니 쓰자는 건 매몰비용이다. 세 가지가 구조적으로 어긋난다.

| | TwinLiteNet+ | 주차가 요구하는 것 |
|---|---|---|
| **입력** | 전방 원근 영상 | **BEV(어라운드뷰) 합성 영상.** 주차칸은 직사각형·직각·규격 치수인데 그 성질이 BEV 에서만 보인다. 원근 영상에서는 거리마다 모양이 바뀌는 사다리꼴이다 |
| **출력** | 픽셀별 drivable / lane | **슬롯 엔티티** — 코너 4점 + 방향 + 점유. 픽셀에서 엔티티로 올리는 게 어려운 부분이고, 주차 문헌은 그걸 **직접 회귀**해서 건너뛴다 |
| **학습 분포** | BDD100K — 주행 중, 원거리, 종방향 차선 | 근거리, 직각, 저속, 후방 |

**그래서 기본 방향은 "TwinLiteNet+ fine-tune"이 아니라 "주차용 모델로 교체"다.**
남는 쓸모는 두 가지뿐이고 둘 다 우선순위가 낮다 — 기하 비교군의 마스크 공급원,
그리고 drivable 영역(어디로 갈 수 있나).

> 이식 자체가 낭비는 아니었다. **학습·평가·데이터 배관이 통째로 돌아가는 상태**가 남았고
> (193개 테스트 통과), 새 모델도 같은 배관에 얹으면 된다. 버리는 건 가중치와 head 정의다.

### 그렇다고 전부 기하로 하면 배우는 게 없다

**인지는 학습, 경로·제어는 기하.** 그리고 **학습 블록마다 기하 베이스라인을 같이 둔다.**
둘을 같은 데이터로 비교한 표가 곧 최종 발표 자료가 된다 — "우리는 두 방식을 다 구현하고
같은 지표로 비교했다"는 1학점 과목에서 충분히 강한 결과다.

| | 학습 쪽 | 기하 비교군 | 담당 |
|---|---|---|---|
| **A. 슬롯 인식 모델** | 모델을 **고르고** 학습·추론한다. 유력 후보는 DMPR-PS 계열 (BEV 입력 → 코너를 위치+방향으로 직접 회귀) | — | **하수영** |
| **B. 모델 출력 → 주차 목표** | (후처리는 기하다. 다만 A 의 출력 표현을 정하는 데 관여한다) | 마스크에 직선 피팅 후 교점 | **강도균** |
| **C. 주차 정책** *(3블록 끝, 여유 시)* | 모방학습 — 수동주차 궤적을 behavior cloning | 후진 pure-pursuit | 안승현 |

A 와 B 는 **네트워크와 그 뒤의 기하**로 나뉜다. DMPR-PS 자신도 `inference.py` 가
`detect_marking_points`(네트워크) → `inference_slots`(기하 페어링) 두 단계다. 그 경계에서
자른 것이다.

**B 의 기하 비교군이 곧 학습 ↔ 기하 비교의 대조군이다.** 같은 BEV 에서 한쪽은 네트워크로
코너를 찍고(A), 한쪽은 선을 맞춰 교점을 낸다(B 비교군). 같은 지표로 재면 그게 발표 자료다.

C 는 **M3 가 끝난 뒤에만 시작한다.** 안 되면 안 하는 것이고, 되면 발표에서 가장 눈에 띈다.

### DMPR-PS 가 유력 후보인 이유 — 다만 **고르는 것은 하수영이다**

- 네트워크가 작다 (`depth_factor 32`, 기본 20 epoch / batch 24). 4060 Ti 로 충분하다
- **출력이 눈에 보인다.** 영상에 점과 화살표가 찍힌다. 초보자가 디버깅할 수 있는 형태다
- **사전학습 가중치가 공개돼 있다.** 백지 학습이 아니라 fine-tune 으로 시작한다
- 라벨이 점 하나당 숫자 4개(x, y, 방향, 모양)뿐이라 **자동 생성이 현실적이다** (§3)

> 이건 내가 조사해서 내린 **잠정 1순위**일 뿐이다. 최종 선택은 하수영이 근거와 함께 정한다
> (§7). 다른 결론이 나오면 그게 맞다 — 다만 **"BEV 입력 + 슬롯 엔티티 출력"이라는 형태**는
> 주차 문헌 전체가 공유하는 것이라, 거기서 벗어나려면 이유가 있어야 한다.

### 우리 BEV 를 DMPR-PS 스케일에 맞출 수 있다 — 검증 필요

`DMPR-PS/config.py` 의 거리 상수들에서 ps2.0 AVM 영상의 물리 크기를 역산했다.
`LONG_SEPARATOR_LENGTH = 0.46875` 를 슬롯 깊이 5 m 로 놓으면 **영상 한 변 ≈ 10.7 m**이고,
그때 나머지 셋이 전부 실제 주차장 치수에 떨어진다:

| 상수 | 역산값 | 실제 |
|---|---|---|
| `SHORT_SEPARATOR_LENGTH` | 2.13 m | 주차칸 폭 ✓ |
| `VSLOT_MIN/MAX_DIST` (제곱거리) | 2.26 ~ 3.54 m | 직각주차 입구 폭 ✓ |
| `HSLOT_MIN/MAX_DIST` (제곱거리) | 4.14 ~ 7.11 m | 평행주차 길이 ✓ |

**따라서 우리 BEV 를 `x[-5.35, 5.35] y[-5.35, 5.35]` 정사각으로 잘라 512×512 로 렌더하면
ps2.0 과 같은 스케일이 되고, 사전학습 가중치와 `config.py` 의 임계값을 손대지 않고 쓸 수
있다.** 우리 격자(`x[-10,10] y[-8,8]`) 안에 들어가므로 crop 만 하면 된다.

> 이건 **상수에서 역산한 추정**이다. 강도균의 첫 과제(§7)에서 ps2.0 실물로 확인한다.
> 틀리면 crop 크기만 바꾸면 되므로 위험이 작다.

---

## 3. 재학습이 필요해지면, 병목은 모델이 아니라 **라벨**이다

학습 세 개가 전부 데이터를 요구하는데 **손으로 라벨링하면 7주 안에 못 한다.**
DMPR-PS 원저자도 전용 라벨링 툴을 따로 만들었다.

**우리는 시뮬레이터 안에 있으니 정답을 이미 안다.**

```
MORAI Semantic PNG  +  지정 칸의 폴리곤  +  /Ego_topic pose
        └─ 4대 카메라 → BEV 투영 → (A) 세그 마스크 라벨
                                   └─ 칸 코너 4점 → (B) DMPR-PS marking point 라벨
```

즉 **주차장에서 지정 칸 주변을 수동주행하기만 하면 A·B 의 라벨이 동시에 나온다.**
수집 파이프라인은 이미 이식돼 있고([data_collection](../src/data_collection/README.md)),
여기에 **BEV 라벨 생성 한 단계만 붙이면 된다.**

### 다만 — **지금 당장 필요한지는 아직 모른다**

DMPR-PS 는 사전학습 가중치가 공개돼 있다. 그게 우리 BEV 에 **그대로(zero-shot) 먹히면
수집도 라벨도 당분간 필요 없다.** ps2.0 도 주차장 AVM 영상이라 도메인이 아주 멀지는 않다.

그래서 순서를 이렇게 잡는다.

1. **먼저 사전학습 가중치를 우리 BEV 에 그대로 돌려 본다** (하수영, 1블록)
2. 쓸 만하면 → 수집·라벨은 **뒤로 미룬다.** 남는 시간을 M2·M3 에 쓴다
3. 부족하면 → **그때 김동현이 자동 라벨 생성기를 만든다.** 이게 재학습의 전제다

**즉 "재학습이 필요한가"가 먼저 답해야 할 질문이고, 그 답이 김동현의 투입 시점을 정한다.**
그때까지 김동현은 §5 의 다른 축(측정·평가)을 맡는다.

### 먼저 확인해야 할 것 하나

**MORAI Semantic 팔레트에 주차칸 선 전용 클래스가 없다.** 슬롯 라인이 `white_lane` 으로
칠해지는지, `yellow_lane` 인지, 아예 안 칠해지는지 **아직 아무도 모른다.**

- 칠해진다 → 위 파이프라인이 그대로 돈다
- 안 칠해진다 → **지정 칸 폴리곤을 BEV 에 투영해서 선을 직접 그린다.** 칸이 하나뿐이라
  이 대안이 오히려 간단하다. 이것도 시뮬레이터 GT 라 정답이다

**어느 쪽이든 막히지 않는다.** 다만 답에 따라 김동현의 작업량이 달라지므로 1블록에 확인한다
(`scripts/audit_semantic_capture.py`).

---

## 4. 원래 분담과의 대응

| 팀원 | 처음 배분 | 이 계획에서 | 달라진 점 |
|---|---|---|---|
| **하수영** | 카메라 모델 — TwinLiteNet+ 가 주차에 부적절하면 다른 모델을 찾아 적용 | **인지 모델 오너** — 고르고, 돌리고, 필요하면 학습한다 | **그대로다.** §2 의 판정이 그 질문에 대한 내 답이고, 최종 결정은 본인이 한다 |
| **강도균** | 차선 피팅 | **모델 출력 → 주차 목표 자세** + 기하 비교군 + 지정 칸 GT | 같은 문제의 한 단계 위. 선을 맞추는 일이 "코너를 짝지어 슬롯을 만드는 일"이 된다 |
| **김동현** | 데이터 파이프라인 (재학습이 필요하면) | **측정·평가 (지금)** → 데이터 파이프라인 (재학습이 결정되면) | 재학습 여부가 아직 미정이라 §3 참조. 그동안 비어 있던 축을 맡는다 |
| **장원태** | — | **이식·프로젝트 관리 (안승현과 함께)** | 초기 개발 분담에서 제외. 1챕터 후 재배분 |
| **안승현** | — | 이식 총괄 · BEV · 통합 · M1 배선 | — |

### 세 사람의 경계

셋 다 "주차칸을 본다"라 그냥 두면 겹친다. **파이프라인의 단계로 자른다.**

```
BEV 영상  ──►  [ A. 네트워크 ]  ──►  코너 점들  ──►  [ B. 기하 ]  ──►  목표 자세
  (안승현)         하수영                             강도균              │
                                                                          ▼
                                                          [ C. 측정 ]  김동현
                                                       오차 · 성공률 · 실패 원인
```

- **하수영** = 모델. 입력을 받아 **점을 찍는 것**까지
- **강도균** = 그 점들로 **"어느 칸이 어디에 어떤 방향으로 있다"를 만드는 것**
- **김동현** = 그게 **얼마나 맞았는지 재는 것**

김동현의 측정이 없으면 하수영·강도균은 자기가 잘하고 있는지 알 수 없다. **그래서 측정이
먼저다.**

---

## 5. 역할과 경로 오너십

| 담당 | 영역 | 경로 |
|---|---|---|
| **하수영** | **인지 모델** — 선정 · 추론 · (필요 시) 학습 | `src/perception/slot_detector/`(신규), `weights/`, `src/perception/twinlite_morai/` |
| **강도균** | **슬롯 기하** — 코너 → 목표 자세 · 기하 비교군 · 지정 칸 GT · 센서셋 | `src/perception/slot_geometry/`(신규), `src/vip3_hd_map/config/vip3_parking_spaces.json`, `config/VIP3_sensor_set_*.json` |
| **김동현** | **측정·평가** → (재학습 결정 시) 데이터 파이프라인 | `src/vip3_eval/`(신규), `src/data_collection/` |
| **안승현** | BEV 합성 · 통합 · 시각화 · M1 배선 | `src/perception/drivable_bev/`, `src/vip3_hd_map/`, `src/perception/camera_semantic_perception/`, `src/vip3_control/`(신규), `tools/` |
| **장원태** | 이식 · 프로젝트 관리 · 공용 인프라 | `docker/`, `scripts/`, `config/`, `src/vip3_bringup/`, `src/vip3_vehicle_state/` |
| 공동 | 바뀌면 전원이 멈추는 것 | `config/vip3_topics.yaml`, `config/VIP3_network_v1.json`, `src/vip3_msgs/`, `src/morai_msgs` |

**자기 경로 밖을 고칠 때만** 사전에 합의한다. 그 외에는 바로 `main` 에 push 한다.

> **판단·제어는 1챕터 후에 재배분한다.** 센서와 인지의 윤곽이 나와야 경로 생성기의 입력이
> 정해지기 때문이다. 그때까지 M1 의 제어는 **연구 과제가 아니라 배선**으로 취급해서
> 안승현이 최소 구현만 한다 (§7).

---

## 6. 블록별 계획

> 일정 근거는 [course.md](course.md) §3. 2026-09-23 공지 기준이고
> **발표가 10/13 · 11/17 · 12/15 세 번**이다.

### 1블록 — 남은 2주 (09/21 ~ 10/04)

목표: **M1 + 인지 모델 후보 확정 + 측정 도구.**

| # | 일 | 담당 | 공수 | 완료 판정 |
|---:|---|---|---:|---|
| 1 | 26.R1 네트워크 세팅, 카메라 4대 토픽 확인 | 안승현·장원태 | 0.5d | `verify_morai_topics.py` 통과 |
| 2 | **`/ctrl_cmd` 켜기 + 기어 R 확인** | 장원태 | 0.5d | 차가 후진한다 |
| 3 | **지정 칸 1개 좌표 + 시작 자세 확정** | 강도균 | 0.5d | `vip3_parking_spaces.json` 에 칸 1개, RViz 에 뜬다 |
| 4 | BEV 지면 격자 게이트 — 격자가 아스팔트에 얹히는가 | 안승현 | 1d | 스크린샷 |
| 5 | **후진 pure-pursuit + 손으로 적은 경유점** | 안승현 | 2d | **M1** |
| 6 | **`vip3_eval`** — 코너오차·heading오차·소요시간·충돌 | 김동현 | 2d | M1 의 숫자가 CSV 로 나온다 |
| 7 | **인지 모델 후보 3개 비교 → 1개 선정** | 하수영 | 3d | 근거가 적힌 결정 (§7) |
| 8 | **선정 모델을 공개 데이터로 재현** | 하수영 | 2d | 검출 그림 한 장 |
| 9 | **코너 → 목표 자세 변환기** (순수 함수, 시뮬 불필요) | 강도균 | 2d | 단위 테스트 통과 |
| 10 | Semantic 주차칸 클래스 확인 | 김동현 | 0.5d | §3 의 답 |

> 5·6번이 M1 이고 7·8·9번이 인지의 출발점이다. **둘은 독립**이라 동시에 간다.

### 10/05 ~ 10/13 — **중간발표-1 준비** (시험 기간)

**새 기능을 시작하지 않는다.** 10/04 까지 만든 것을 발표 자료로 만든다.

- M1 시연 영상 + `vip3_eval` 오차 표
- 인지 모델 선정 근거 (하수영) — 발표에서 가장 설명하기 좋은 부분이다
- 남은 계획

### 2블록 — 1주 (10/26 ~ 11/01)

바로 다음 주가 중간고사다. 큰 작업을 시작하지 않는다.

- 빌드·연결 재확인 (10/04 커밋이 온전하면 반나절)
- **선정 모델을 우리 BEV 에 zero-shot 으로 돌려 본다** (하수영) — §3 의 분기점
- 그 결과로 **재학습 필요 여부 결정** → 김동현의 투입 시점이 여기서 정해진다

### 3블록 — 3주 (11/09 ~ 11/29) + 마무리 1주 (~12/06)

| 주 | 목표 |
|---|---|
| 11주 (~11/15) | **M2.** 인지가 칸 자세를 출력하고 오픈루프 오차가 나온다 |
| **11/17** | **중간발표-2** — M2 결과 |
| 12주 (~11/22) | **M3.** 인지 출력이 M1 의 목표 자세를 대체. 성공률 측정 |
| 13주 (~11/29) | 실패 케이스 분석 · **학습 ↔ 기하 비교표** |
| 14주 (~12/06) | 안정화. **12/06 기능 동결** |

여유가 있으면 C(모방학습)를 붙인다. **M3 가 안 됐으면 C 는 하지 않는다.**

---

## 7. 각자의 첫 과제

**셋 다 시뮬 연결을 기다리지 않는다.** 공개 데이터·공개 가중치·순수 함수로 시작해서,
우리 데이터가 준비되면 입력만 바꿔 끼운다.

### 하수영 — 인지 모델을 고른다 (이 팀에서 가장 중요한 결정)

**질문:** 주차칸을 인식할 모델로 무엇을 쓸 것인가. TwinLiteNet+ 는 왜 아닌가(또는 왜 맞는가).

§2 에 내 판정을 적어 뒀지만 **그건 참고이고 결정은 본인이 한다.**
`../external/parking/` 에 9개가 clone 돼 있고
[README](../../external/parking/README.md) 에 검증 결과·논문 링크가 정리돼 있다.

**읽을 것 3편** (더 읽지 않는다 — 고르는 게 목적이지 다 읽는 게 목적이 아니다):

1. 서베이 — *Review of Vision-Based Deep Learning Parking Slot Detection on Surround View Images* (2023). 슬롯 검출 계열 전체 지형
2. `e2e-parking-carla` (IV 2024) — PDF 가 clone 안에 있다. **시뮬 기반이라 우리와 조건이 같다**
3. `ParkingE2E` (IROS 2024) — 같은 팀의 실차 + ROS1 판

**후보를 비교할 때 반드시 같이 적을 것:**

| 항목 | 왜 |
|---|---|
| **입력 형태** | 원근 4장 / BEV 합성 1장 / fisheye — **이게 센서셋을 결정한다** |
| **출력 표현** | 픽셀 마스크 / 코너 점 / 슬롯 박스 — 강도균이 받을 형태 |
| **사전학습 가중치** | 있으면 재학습을 피할 수 있다. **7주 프로젝트에서 이게 가장 크다** |
| 학습 데이터 | 우리가 만들 수 있는 라벨인가 |
| 모델 크기 | RTX 4060 Ti 8 GB 로 되는가 |

**완료 판정:** 위 표를 채운 후보 3개 비교 + 1개 선정 + 그 근거. `docs/notes/` 에 적는다.
**그 다음** 선정 모델을 공개 데이터로 돌려 검출 그림 한 장을 뽑는다.

> 지금 저장소의 TwinLiteNet+ 가중치·테스트는 그대로 둔다. 비교군으로 쓸 수 있고,
> 무엇보다 **학습·평가 배관이 살아 있는 상태**라 새 모델을 얹을 자리가 된다.
> 절차는 [twinlite_morai/README.md](../src/perception/twinlite_morai/README.md).

### 강도균 — 코너 점들을 "주차 목표 자세"로 바꾼다

모델이 코너 점을 몇 개 찍어 준다. 그걸로 **"지정한 칸이 내 기준 어디에 어떤 방향으로
있는가"**를 만들어야 차를 보낼 수 있다. 이게 원래 맡은 "차선 피팅"의 한 단계 위다.

**시뮬레이터도 모델도 없이 지금 시작할 수 있다.** 입력을 손으로 만들면 된다.

```python
# 입력: 코너 점 리스트 [(x, y, 방향), ...]  (base_link 기준 미터)
# 출력: 목표 자세 (x, y, yaw) + 신뢰도
def slot_pose_from_corners(points, expected_width_m=2.5, expected_length_m=5.0):
    ...
```

다뤄야 할 경우들 — **이게 실제 난이도다:**

- 점이 4개 다 보일 때 / **2개만 보일 때** (후진 중엔 앞쪽 두 점이 시야 밖이다)
- 점이 5개 이상 나올 때 (옆 칸의 점이 섞인다) → 규격 치수로 걸러낸다
- 어느 두 점이 **입구 변**인가 (차가 들어가는 쪽)
- 방향이 180도 뒤집혔을 때

**완료 판정:** 위 경우들에 대한 단위 테스트가 통과. 시뮬 불필요.
기존 패키지들이 전부 이 형태다 (`python3 -m unittest`).

**동시에:** 지정 칸 1개의 좌표를 `vip3_parking_spaces.json` 에 넣는다. 지금 `[]` 로 비어
있고 **팀 전체의 유일한 주차 GT 가 될 값**이다. 칸 고르는 기준·좌표 찍는 방법은
[SCHEMA.md](../src/vip3_hd_map/config/vip3_parking_spaces.SCHEMA.md).

### 김동현 — "주차 성공"을 숫자로 정의한다

**성공의 정의가 없으면 M1 이 됐는지 아무도 모르고, 하수영·강도균도 자기가 나아지고 있는지
모른다.** 그래서 이게 첫 과제다.

입력은 `/Ego_topic` pose + 지정 칸 폴리곤, 출력은 CSV 한 줄:

```
run_id, 코너최대오차_m, heading오차_deg, 소요시간_s, 충돌횟수, 성공여부
```

**시뮬 없이 만든다** — pose 와 폴리곤을 넣으면 숫자가 나오는 **순수 함수가 먼저**고,
ROS 노드는 그 위에 씌운다.

**완료 판정:** 아래 세 경우가 각각 맞는 숫자를 내는 단위 테스트.

| 경우 | 기대 |
|---|---|
| 칸 정중앙, 정렬됨 | 코너오차 ≈ 0, heading오차 ≈ 0, 성공 |
| 30 cm 밀림 | 코너오차 ≈ 0.30 |
| 10도 틀어짐 | heading오차 ≈ 10 |

**그 다음 — 성공 기준선을 정한다.** "코너오차 몇 cm 이내, heading 몇 도 이내면 성공인가."
이건 팀 전체가 쫓을 숫자이므로 근거와 함께 제안하고 합의한다
(참고: 실제 주차장 칸 폭 2.5 m, 차폭 1.892 m → 좌우 여유 30 cm 씩).

**이후 (재학습이 결정되면):** 자동 라벨 생성기 → §3.

### 안승현 — 남의 블로커부터 치운다

1. 26.R1 네트워크 세팅 + `/ctrl_cmd` (2·5번의 전제)
2. BEV 지면 격자 게이트 — 틀리면 그 뒤가 전부 조용히 무의미해진다
3. **M1 배선** — 후진 pure-pursuit ~200줄 + 손으로 적은 경유점.
   ASMC 의 `mpc_controller` 는 재사용할 게 없다 (`grep gear` 0건, 곡률 상한이
   `tan(40°)/wheelbase` 라 주차 경로는 상시 거절). 0.5~1.5 m/s 에서 MPC 의 예측 지평
   이득은 사실상 0 이다. `δ = atan(2·L·sin α / Ld)`, 후진은 뒷축 기준 부호 반전.
   **26.R1 에서는 `front_steer` 에 넣고 `rear_steer = 0.0` 을 명시적으로 채운다**
   ([msgs-26r1.md](msgs-26r1.md) §2)
4. **모델 입력용 정사각 BEV crop** — 하수영이 고른 모델의 입력 규격에 맞춰 (§2)

### 장원태 — 이식·관리 (초기 개발 분담 제외)

안승현과 함께 이식 마무리와 프로젝트 관리를 맡는다. **1챕터가 끝나면 판단·제어 쪽으로
재배분**한다 — 경로 생성기의 입력이 인지에서 나오므로 그 윤곽이 먼저다.

## 8. 안 하는 것과 그 이유

| 대상 | 왜 |
|---|---|
| **빈 칸 탐색 · 다중 슬롯 선택** | §1. 7주에 안 된다. 칸은 지정한다 |
| **평행주차** | 직각주차 하나만. 되면 그때 본다 |
| end-to-end 주차 정책 (C) | M3 가 끝난 뒤에만. 중간 출력이 없어 디버깅이 안 된다 |
| **TwinLiteNet+ 를 주차선용으로 fine-tune** | §2. 입력·출력·학습분포가 전부 어긋난다. 배관은 남기고 **모델은 교체**한다 |
| AVM 스티칭 코드 이식 | `drivable_bev` 가 이미 카메라별 지면 호모그래피 + 품질 가중 블렌딩을 한다. **그게 곧 AVM 이다** |
| `frenet_planner` / `mpc_controller` / `behavior_planner` | 고속 주행용. §7 장원태 항목 |
| 신호등 · 보행자 · 충돌회피 | 강의에는 나오지만 주차 범위 밖 |
| SVM 센서셋(v2) 전환 | 계산·JSON 은 만들어 뒀다([sensors.md](sensors.md) §5). **하수영이 고른 모델의 입력 형태가 정해진 뒤에 판단한다** — fisheye AVM 을 전제하는 모델을 고르면 전환이 필수가 되고, 핀홀 BEV 로 가면 지금 센서셋으로 간다 |

---

## 9. 알려진 위험 4개

| 위험 | 지금 상태 | 대응 |
|---|---|---|
| **인지 모델 선정이 늦으면 전부 밀린다** | 1블록 과제 | 하수영. 10/13 발표에서 "무엇을 왜 골랐나"가 핵심 내용이 된다. **후보를 다 읽으려 하지 말고 3편에서 끊는다** |
| **`/ctrl_cmd` 가 네트워크 프리셋에 없다** | 확인됨 | 1블록 2번. **이게 안 되면 주차 자체가 불가능**하다 |
| **`parking_space_set.json` 을 못 받았다** | MORAI 원본에는 있었는데 전달 누락 ([katri-map.md](katri-map.md)) | 메일로 요청(장원태, 30분). 못 받아도 **칸 1개는 손으로 찍으면 된다**(강도균) |
| **Semantic 에 주차칸 선 클래스가 없을 수 있다** | 미확인 | §3. 안 칠해지면 폴리곤을 직접 투영. **칸이 하나라 대안이 더 간단하다** |

---

## 10. 나중에 볼 만한 것

- `tools/aim_scenario_runner` — 주차 시작 자세 반복 재현. 수집·평가 반복성에 큰 이득이지만
  gRPC 의존이 붙는다. **ROS 대안 먼저**: `/ScenarioLoad` 로 고정 시작 상태 리로드
- 조향·페달 캘리브레이션 — 저속·후진 δ→κ 와 브레이크 데드밴드 실측 (0.5d). 제어기의 입력
- LiDAR 점유로 BEV 보정 — 지면 평면 가정 때문에 주차된 옆 차가 시선 방향으로 길게 번진다.
  `/velodyne_points` 는 이미 있다
