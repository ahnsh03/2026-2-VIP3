# 역할 분담과 작업 계획

> **문서 역할:** 제안 — 팀 논의용. 합의되면 [architecture.md](architecture.md) §5 에 반영한다
> **담당:** 안승현
> **최종 수정:** 2026-09-23

## 0. 이 문서를 읽는 법

**팀원 각자 §5 에서 자기 이름을 찾고, §7 의 "첫 과제"부터 시작하면 된다.**
§1~§4 는 왜 그렇게 나눴는지의 근거다.

읽기 전 전제 두 개:

- 개발 가능 주는 **3·4·5 / 9 / 11·12·13 — 흩어진 7주** ([course.md](course.md) §3).
  1블록은 3주차가 지나서 **실질 2주**(~10/04)뿐이고, 10/05~11/01 은 시험으로 끊긴다
- ASMC 이식은 끝났다 ([porting-from-asmc.md](porting-from-asmc.md)). 빈 저장소가 아니라
  **BEV·수집·학습 뼈대가 이미 도는 상태**에서 시작한다 (테스트 193개 통과)

---

## 1. 목표를 최소 단위로 자른다

**주차칸을 고르지 않는다. 칸 하나를 미리 지정해 두고, 그 칸을 인식해서 주차한다.**

빈 칸 탐색·다중 후보 선택·주차장 순회는 **전부 범위 밖**이다. 논문들이 푸는 문제의
대부분이 "어느 칸에 댈지 고르는 것"인데, 그건 7주에 팀 전체가 매달려도 시연까지 못 간다.
게다가 **틀렸을 때 어디가 틀렸는지 알 수가 없다.**

### 마일스톤 3개

| | 이름 | 무엇이 되면 끝인가 | 인지 | 목표 시점 |
|---|---|---|---|---|
| **M1** | 인식 없이 주차 | 고정 시작자세 → 손으로 적은 경유점 → 차가 지정 칸에 들어간다 | 0 % | 1블록 끝 (10/04) |
| **M2** | 보기만 한다 | 차가 주차칸 선을 **인식해서 칸 자세를 출력**한다. 정답과의 오차를 숫자로 잰다 | 100 % (오픈루프) | 3블록 1주차 (11/15) |
| **M3** | 본 것으로 주차 | M2 의 출력이 M1 의 목표 자세를 대체한다. 성공률을 잰다 | 100 % (폐루프) | 11/29 기능 동결 |

**M1 에 인지가 0 %인 것이 핵심이다.** 차를 먼저 움직여 놓으면, 그 뒤 인지가 틀렸을 때
"인지가 틀렸다"고 바로 말할 수 있다. 반대로 하면 끝까지 아무것도 못 움직인다.

M1 은 전체 배선(제어·평가·시작자세 재현)을 다 태우면서도 **학습이 하나도 필요 없다.**
그 사이에 나머지 셋이 학습 쪽을 병렬로 판다.

> **왜 M1 이 시시해 보여도 중요한가.** 12/08 시연에서 "차가 칸에 들어간다"가 안 되면
> 인지가 아무리 좋아도 보여줄 게 없다. M1 을 10/04 에 끝내 두면 **최악의 경우에도
> 시연할 것이 남는다.** 그 뒤는 전부 개선이다.

---

## 2. 학습 기반을 어디에 넣나

### 전부 학습으로 하면(end-to-end) 7주에 디버깅이 안 된다

`e2e-parking-carla`(IV 2024)·`ParkingE2E`(IROS 2024)가 그 방식이다. 영상을 넣으면 조향·페달이
나온다. 멋있지만 **중간 출력이 없어서 틀렸을 때 볼 게 없고**, 두 논문 다 시뮬에서 수만 개
에피소드를 굴려 학습했다.

### 그렇다고 전부 기하로 하면 배우는 게 없다

**인지는 학습, 경로·제어는 기하.** 그리고 **학습 블록마다 기하 베이스라인을 같이 둔다.**
둘을 같은 데이터로 비교한 표가 곧 최종 발표 자료가 된다 — "우리는 두 방식을 다 구현하고
같은 지표로 비교했다"는 1학점 과목에서 충분히 강한 결과다.

| | 학습 쪽 | 기하 베이스라인 (비교군) | 담당 |
|---|---|---|---|
| **A. BEV 세그멘테이션** | TwinLiteNet+ 를 주차선 클래스로 fine-tune | MORAI Semantic 색 매칭 | 하수영 |
| **B. 슬롯 포인트 검출** | DMPR-PS — 코너를 (위치+방향)으로 직접 회귀 | A 의 마스크에 직선 피팅 후 교점 | 강도균 |
| **C. 주차 정책** *(3블록, 여유 시)* | 모방학습 — 수동주차 궤적을 behavior cloning | 후진 pure-pursuit | 안승현·장원태 |

A 와 B 는 **입력이 같고(BEV) 출력 표현만 다르다.** 마스크를 뽑아 선을 맞출 것인가,
코너를 바로 찍을 것인가. 이 비교가 프로젝트의 학술적 축이다.

C 는 **M3 가 끝난 뒤에만 시작한다.** 안 되면 안 하는 것이고, 되면 발표에서 가장 눈에 띈다.

### B 를 고른 이유 — DMPR-PS 는 이 팀에 맞는 크기다

- 네트워크가 작다 (`depth_factor 32`, 기본 20 epoch / batch 24). 4060 Ti 로 충분하다
- **출력이 눈에 보인다.** 영상에 점과 화살표가 찍힌다. 초보자가 디버깅할 수 있는 형태다
- **사전학습 가중치가 공개돼 있다.** 백지 학습이 아니라 fine-tune 으로 시작한다
- 라벨이 점 하나당 숫자 4개(x, y, 방향, 모양)뿐이라 **자동 생성이 현실적이다** (§3)

### 우리 BEV 를 DMPR-PS 스케일에 맞출 수 있다 — 검증 필요

`DMPR-PS/config.py` 의 거리 상수들에서 ps2.0 AVM 영상의 물리 크기를 역산했다.
`LONG_SEPARATOR_LENGTH = 0.46875` 를 슬롯 깊이 5 m 로 놓으면 **영상 한 변 ≈ 10.7 m**이고,
그때 나머지 셋이 전부 실제 주차장 치수에 떨어진다:

| 상수 | 역산값 | 실제 |
|---|---|---|
| `SHORT_SEPARATOR_LENGTH` | 2.13 m | 주차칸 폭 ✓ |
| `VSLOT_MIN/MAX_DIST` (제곱거리) | 2.26 ~ 3.54 m | 직각주차 입구 폭 ✓ |
| `HSLOT_MIN/MAX_DIST` (제곱거리) | 4.14 ~ 7.11 m | 평행주차 길이 ✓ |

**따라서 우리 BEV 를 `x[-5.35, 5.35] y[-5.35, 5.35]` 정사각으로 잘라 512×512 로 렌더하면
ps2.0 과 같은 스케일이 되고, 사전학습 가중치와 `config.py` 의 임계값을 손대지 않고 쓸 수
있다.** 우리 격자(`x[-10,10] y[-8,8]`) 안에 들어가므로 crop 만 하면 된다.

> 이건 **상수에서 역산한 추정**이다. 강도균의 첫 과제(§7)에서 ps2.0 실물로 확인한다.
> 틀리면 crop 크기만 바꾸면 되므로 위험이 작다.

---

## 3. 크리티컬 패스는 모델이 아니라 **자동 라벨**이다

학습 세 개가 전부 데이터를 요구하는데 **손으로 라벨링하면 7주 안에 못 한다.**
DMPR-PS 원저자도 전용 라벨링 툴을 따로 만들었다.

**우리는 시뮬레이터 안에 있으니 정답을 이미 안다.**

```
MORAI Semantic PNG  +  지정 칸의 폴리곤  +  /Ego_topic pose
        └─ 4대 카메라 → BEV 투영 → (A) 세그 마스크 라벨
                                   └─ 칸 코너 4점 → (B) DMPR-PS marking point 라벨
```

즉 **주차장에서 지정 칸 주변을 수동주행하기만 하면 A·B 의 라벨이 동시에 나온다.**
수집 파이프라인은 이미 이식돼 있고([data_collection](../src/data_collection/README.md)),
여기에 **BEV 라벨 생성 한 단계만 붙이면 된다.**

**이게 김동현의 핵심 과제이고 팀 전체의 크리티컬 패스다.** 이게 늦으면 하수영·강도균이
둘 다 멈춘다. 그래서 §7 에서 두 사람의 첫 과제를 **공개 데이터셋으로** 잡아 의존을 끊었다.

### 먼저 확인해야 할 것 하나

**MORAI Semantic 팔레트에 주차칸 선 전용 클래스가 없다.** 슬롯 라인이 `white_lane` 으로
칠해지는지, `yellow_lane` 인지, 아예 안 칠해지는지 **아직 아무도 모른다.**

- 칠해진다 → 위 파이프라인이 그대로 돈다
- 안 칠해진다 → **지정 칸 폴리곤을 BEV 에 투영해서 선을 직접 그린다.** 칸이 하나뿐이라
  이 대안이 오히려 간단하다. 이것도 시뮬레이터 GT 라 정답이다

**어느 쪽이든 막히지 않는다.** 다만 답에 따라 김동현의 작업량이 달라지므로 1블록에 확인한다
(`scripts/audit_semantic_capture.py`).

---

## 4. 원래 분담과의 대응

| 팀원 | 처음 배분 | 이 계획에서 | 왜 |
|---|---|---|---|
| 강도균 | 차선 피팅 | **학습 기반 슬롯 포인트 검출 (B)** | 같은 문제의 학습 버전. 기하 피팅은 비교군으로 남는다 |
| 김동현 | 데이터 파이프라인 | **데이터 파이프라인 + 자동 라벨 + 평가** | 그대로 + 라벨 생성이 추가 |
| 하수영 | 카메라 모델 | **BEV 세그멘테이션 학습 (A) + 기반 논문** | 그대로. 학습 파이프라인이 이미 있다 |
| 장원태 | — | 인프라·시뮬 연동 + **후진 제어** | 실험자 2명 중 1명. M1 을 책임진다 |
| 안승현 | — | BEV 합성 + 통합 + (3블록) 모방학습 | 팀장. 남의 블로커를 먼저 치운다 |

**강도균과 하수영의 경계를 명확히 한다** — 둘 다 "선을 본다"라 겹치기 쉽다.

> **하수영 = 픽셀 (마스크). 강도균 = 점 (코너 4개).**
> 하수영의 출력이 강도균의 기하 베이스라인 입력이 되지만, 강도균의 학습 경로(DMPR-PS)는
> **하수영을 거치지 않고 BEV RGB 를 직접 받는다.** 그래서 서로를 기다리지 않는다.

---

## 5. 역할과 경로 오너십

| 담당 | 영역 | 경로 |
|---|---|---|
| **안승현** | BEV 합성(AVM) · 시각화 · 통합 · 모방학습(C) | `src/perception/drivable_bev/`, `src/vip3_hd_map/`, `src/perception/camera_semantic_perception/`, `tools/` |
| **장원태** | 공용 인프라 · 시뮬 연동 · **후진 제어** | `docker/`, `scripts/`, `config/`, `src/vip3_bringup/`, `src/vip3_vehicle_state/`, `src/vip3_control/`(신규) |
| **김동현** | 데이터 파이프라인 · **자동 라벨** · 평가 하네스 | `src/data_collection/`, `src/vip3_eval/`(신규) |
| **하수영** | **BEV 세그멘테이션 학습 (A)** · 기반 논문 | `src/perception/twinlite_morai/`, `scripts/*twinlite*`, `weights/` |
| **강도균** | **슬롯 포인트 검출 학습 (B)** · 지정 칸 GT · 센서셋 | `src/perception/slot_detector/`(신규), `src/vip3_hd_map/config/vip3_parking_spaces.json`, `config/VIP3_sensor_set_*.json` |
| 공동 | 바뀌면 전원이 멈추는 것 | `config/vip3_topics.yaml`, `config/VIP3_network_v1.json`, `src/vip3_msgs/`, `src/morai_msgs` |

**자기 경로 밖을 고칠 때만** 사전에 합의한다. 그 외에는 바로 `main` 에 push 한다.

---

## 6. 블록별 계획

### 1블록 — 남은 2주 (~10/04) : "M1 + 각자 첫 학습을 돌려본다"

| # | 일 | 담당 | 공수 | 완료 판정 |
|---:|---|---|---:|---|
| 1 | 시뮬 연결 확정 — 26.R1 네트워크 세팅, 카메라 4대 토픽 | 안승현·장원태 | 0.5d | `verify_morai_topics.py` 통과 |
| 2 | **`/ctrl_cmd` 켜기 + 기어 R 확인** | 장원태 | 0.5d | 차가 후진한다 |
| 3 | **지정 칸 1개 좌표 확정** | 강도균 | 0.5d | `vip3_parking_spaces.json` 에 칸 1개, RViz 에 뜬다 |
| 4 | BEV 지면 격자 게이트 — 격자가 아스팔트에 얹히는가 | 안승현 | 1d | 눈으로 확인 + 스크린샷 |
| 5 | **후진 pure-pursuit** + 손으로 적은 경유점 | 장원태 | 2d | **M1 달성** |
| 6 | **`vip3_eval`** — 코너오차·heading오차·소요시간·충돌 | 김동현 | 1.5d | M1 의 숫자가 CSV 로 나온다 |
| 7 | Semantic 주차칸 클래스 확인 | 김동현 | 0.5d | §3 의 답 |
| 8 | 지정 칸 주변 수동주행 수집 1회 | 김동현 | 0.5d | 프레임 300장 + `_SUCCESS` |
| 9 | **ps2.0 로 DMPR-PS 재현** | 강도균 | 2d | 논문 수치 근처 + 검출 그림 |
| 10 | **TwinLiteNet+ 추론 그림** (공개 가중치, 우리 영상) | 하수영 | 1.5d | 4뷰 오버레이 |
| 11 | 기반 논문 3편 정독 + 요약 | 하수영 | 상시 | 2블록에 결론 |

> 3·5·6 번이 M1 이고 9·10 번이 학습의 출발점이다. **둘은 서로 독립**이라 동시에 간다.

#### 10/04 전에 반드시 커밋할 것

**"차가 지정 칸에 들어가고, 그 오차가 숫자로 나오는 상태."** 그리고 각자 첫 학습 결과 그림.

10/05~11/01 은 3주 이상 끊긴다. **반쯤 고친 채로 두면 복귀에만 한 주가 든다.**

### 2블록 — 1주 (10/26~11/01) : "복귀 + 계획서 재료"

큰 작업을 시작하지 않는다. 바로 다음 주가 중간고사다.

- 빌드·연결 재확인 (위가 커밋돼 있으면 반나절)
- **하수영: 기반 논문 확정** — A 를 어떤 형태로 갈지 결정
- **김동현: 자동 라벨 생성기 착수** — 3블록의 전제
- 11/10 계획서 발표 자료 정리 (M1 시연 영상 + 오차 표)

### 3블록 — 3주 (11/09~11/29) : "M2 → M3"

| 주 | 목표 |
|---|---|
| 11주 (~11/15) | **M2.** A·B 가 우리 데이터로 학습되고 오픈루프 오차가 나온다 |
| 12주 (~11/22) | **M3.** 인식 출력이 M1 의 목표 자세를 대체. 성공률 측정 |
| 13주 (~11/29) | 실패 케이스 분석 · 학습 ↔ 기하 비교표 · **11/29 기능 동결** |

여유가 있으면 C(모방학습)를 붙인다. **M3 가 안 됐으면 C 는 하지 않는다.**

---

## 7. 각자의 첫 과제 — 시뮬레이터 없이, 오늘 시작할 수 있는 것

**셋 다 시뮬 연결·남의 결과물을 기다리지 않는다.** 공개 데이터와 공개 가중치로 시작해서,
우리 데이터가 준비되면 입력만 바꿔 끼운다.

### 강도균 — DMPR-PS 를 ps2.0 으로 재현한다

```bash
cd ../external/parking/DMPR-PS
# 1) ps2.0 데이터셋 + 라벨 내려받기 (README 의 링크 2개)
# 2) 사전학습 가중치로 추론부터 — 학습 전에 그림이 먼저 나와야 한다
python3 inference.py --mode image --detector_weights <가중치> --inference_slot
# 3) 그 다음 학습
python3 train.py --dataset_directory <준비한 경로> --num_epochs 20
```

**완료 판정:** 주차장 영상에 코너 점과 방향 화살표가 찍힌 그림 한 장.
그리고 `config.py` 의 거리 상수가 실제 몇 미터에 해당하는지 확인 (§2 의 역산 검증).

**그 다음:** 지정 칸 1개의 좌표를 `vip3_parking_spaces.json` 에 넣는다.
형식은 [SCHEMA.md](../src/vip3_hd_map/config/vip3_parking_spaces.SCHEMA.md) 에 있다.
지금 이 파일은 `[]` 로 비어 있고, **팀 전체의 유일한 주차 GT 가 될 값이다.**

### 하수영 — TwinLiteNet+ 를 돌려서 그림을 뽑는다

가중치 2개(v5 binary / v6 4-class)가 `weights/twinlite/` 에 이미 있다. 학습이 아니라
**추론부터** 한다.

```bash
./scripts/docker_train_up.sh build && ./scripts/docker_train_up.sh up
# 단위 테스트로 배관 확인 (데이터 없이 된다)
docker exec vip3-perception-train bash -lc '
  cd /root/ws && PYTHONPATH=src/perception python3 -m unittest discover \
    -s src/perception/twinlite_morai/test -p "test_*.py"'
```

**완료 판정:** v5 와 v6 를 같은 영상에 돌린 비교 그림. 어느 쪽이 주차선을 잘 보는지 의견.
자세한 절차는 [twinlite_morai/README.md](../src/perception/twinlite_morai/README.md).

**동시에:** 논문 3편을 읽는다 — 서베이 1편 → `e2e-parking-carla`(IV 2024, PDF 가 clone 안에
있다) → `ParkingE2E`(IROS 2024). 링크는
[external/parking/README.md](../../external/parking/README.md) §3.

### 김동현 — 평가 하네스를 먼저 만든다 (수집보다 먼저)

**"주차 성공"의 정의가 없으면 M1 이 됐는지 아무도 모른다.** 그래서 이게 첫 과제다.

입력은 `/Ego_topic` pose + 지정 칸 폴리곤, 출력은 CSV 한 줄:

```
run_id, 코너최대오차_m, heading오차_deg, 소요시간_s, 충돌횟수, 성공여부
```

시뮬 없이 만들 수 있다 — **pose 와 폴리곤을 넣으면 숫자가 나오는 순수 함수**가 먼저고,
ROS 노드는 그 위에 씌운다. 단위 테스트로 검증한다 (기존 패키지들이 전부 이 형태다).

**완료 판정:** `python3 -m unittest` 로 "칸 안에 정확히 있는 경우 / 30 cm 밀린 경우 /
10도 틀어진 경우"가 각각 맞는 숫자를 내는 것.

### 장원태 — 후진 제어

ASMC 의 `mpc_controller` 는 **재사용할 게 없다.** 기어 개념이 아예 없고(`grep gear` 0건),
곡률 상한을 `tan(40°)/wheelbase` 로 걸어서 **주차 경로는 상시 거절**된다.

0.5~1.5 m/s 에서는 MPC 의 예측 지평 이득이 사실상 0 이다.
**부호 있는 lookahead pure-pursuit 약 200줄이 1~2일**이면 되고 디버깅도 쉽다.

```
전진:  δ = atan(2·L·sin α / Ld)
후진:  뒷축 기준으로 부호 반전
```

ASMC 에서 가져올 것은 코드가 아니라 설계 두 가지다.

1. **fail-safe** — 입력이 stale/invalid 하거나 해가 없으면 그 사이클은 무조건 STOP
2. **26.R1 에서는 `front_steer` 에 넣고 `rear_steer = 0.0` 을 반드시 명시적으로 채운다**
   ([msgs-26r1.md](msgs-26r1.md) §2)

**완료 판정:** 손으로 적은 경유점을 따라 차가 후진해서 지정 칸에 들어간다 (**M1**).

### 안승현 — 남의 블로커부터 치운다

1. 26.R1 네트워크 세팅 + `/ctrl_cmd` (2·5번의 전제)
2. BEV 지면 격자 게이트 — 틀리면 그 뒤가 전부 조용히 무의미해진다
3. **DMPR-PS 입력용 정사각 BEV crop** (`x[-5.35,5.35] y[-5.35,5.35]` → 512×512) — 강도균의 입력
4. 시각화 — 매주 점검에서 보여줄 그림

---

## 8. 안 하는 것과 그 이유

| 대상 | 왜 |
|---|---|
| **빈 칸 탐색 · 다중 슬롯 선택** | §1. 7주에 안 된다. 칸은 지정한다 |
| **평행주차** | 직각주차 하나만. 되면 그때 본다 |
| end-to-end 주차 정책 (C) | M3 가 끝난 뒤에만. 중간 출력이 없어 디버깅이 안 된다 |
| AVM 스티칭 코드 이식 | `drivable_bev` 가 이미 카메라별 지면 호모그래피 + 품질 가중 블렌딩을 한다. **그게 곧 AVM 이다** |
| `frenet_planner` / `mpc_controller` / `behavior_planner` | 고속 주행용. §7 장원태 항목 |
| 신호등 · 보행자 · 충돌회피 | 강의에는 나오지만 주차 범위 밖 |
| SVM 센서셋(v2) 전환 | 계산·JSON 은 만들어 뒀다([sensors.md](sensors.md) §5). 다만 **TwinLiteNet+ 가중치가 분포 밖이 되어 재학습이 전제**다. A 가 자리잡은 뒤에 판단 |

---

## 9. 알려진 위험 3개

| 위험 | 지금 상태 | 대응 |
|---|---|---|
| **`/ctrl_cmd` 가 네트워크 프리셋에 없다** | 확인됨 | 1블록 2번. **이게 안 되면 주차 자체가 불가능**하다 |
| **`parking_space_set.json` 을 못 받았다** | MORAI 원본에는 있었는데 전달 누락 ([katri-map.md](katri-map.md)) | 메일로 요청(장원태, 30분). 못 받아도 **칸 1개는 손으로 찍으면 된다**(강도균) |
| **Semantic 에 주차칸 선 클래스가 없을 수 있다** | 미확인 | §3. 안 칠해지면 폴리곤을 직접 투영. **칸이 하나라 대안이 더 간단하다** |

---

## 10. 나중에 볼 만한 것

- `tools/aim_scenario_runner` — 주차 시작 자세 반복 재현. 수집·평가 반복성에 큰 이득이지만
  gRPC 의존이 붙는다. **ROS 대안 먼저**: `/ScenarioLoad` 로 고정 시작 상태 리로드
- 조향·페달 캘리브레이션 — 저속·후진 δ→κ 와 브레이크 데드밴드 실측 (0.5d). 제어기의 입력
- LiDAR 점유로 BEV 보정 — 지면 평면 가정 때문에 주차된 옆 차가 시선 방향으로 길게 번진다.
  `/velodyne_points` 는 이미 있다
