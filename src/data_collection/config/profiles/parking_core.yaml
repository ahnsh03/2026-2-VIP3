name: parking_core
description: "주차 기본 수집 — 4카메라(front/left/right/rear) + lidar + ego + gps/imu"
collect_hz: 5
mode: latest_timer
max_frames: 0
require_ego: true
map: R_KR_PG_KATRI
vehicle: 2023_Hyundai_ioniq5
bridge_type: rosbridge
lidar_format: bin_xyzi
notes: |
  rosbridge 단일 WebSocket 으로 카메라 4대가 흐른다. collect_hz 를 올리기 전에
  `rostopic hz` 로 카메라별 실제 수신율을 먼저 확인한다 (docs/simulator.md §5).
  cam_rear 는 후방 카메라다 (1280x720, FOV 90). ASMC 의 하향 cam_extra 가 아니다.

channels:
  ego: true
  objects: true
  collision: true
  lidar: true
  gps: true
  imu: true
  cam_front: true
  cam_left: true
  cam_right: true
  cam_rear: true

topics:
  ego: /Ego_topic
  objects: /Object_topic
  collision: /CollisionData
  lidar: /velodyne_points
  gps: /gps
  imu: /imu
  cam_front: /cam_front/image_jpeg/compressed
  cam_left: /cam_left/image_jpeg/compressed
  cam_right: /cam_right/image_jpeg/compressed
  cam_rear: /cam_rear/image_jpeg/compressed
